Architecture
 Features as optional components of parts
 Visible parts: loaded into iframes
 Invisible parts: ? loaded into iframes?
 Built-in parts vs extension parts: loaded via purple.conf
  purple.conf: plain text, shaved JSON
  
For DOM proxy
  WebKit IDL uses perl script to gen bindings, instrument it?